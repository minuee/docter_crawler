<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sql">  

    <select id="select_doctor_treatise">
        SELECT
            dp.paper_id,dp.doctorName,dp.title,dp.title_hash,dp.paper_url
        FROM 
            doctor_paper dp INNER JOIN doctor_basic db ON dp.rid = db.rid
        WHERE
            1=1 and dp.title IS NOT NULL and dp.pmid IS NULL
            <if test="version_id != null">
                AND dp.data_version_id = #{version_id}
            </if>
            <if test="hid != null">
                AND db.hid = #{hid}
            </if>
        ORDER BY dp.paper_id asc
    </select>

</mapper>